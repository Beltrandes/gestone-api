package com.gestone.gestone_api.quotation;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class QuoteItemTest {

    // @Test
    // public void testCalculate() {
    //     Material material = new Material("Test material", "Test details", new BigDecimal(850), MaterialType.GRANITE, null);
    //     BigDecimal measureX = new BigDecimal("0.60");
    //     BigDecimal measureY = new BigDecimal("0.60");
    //     Integer quantity = 2;
    //     QuoteItem quoteItem = new QuoteItem("Item 1", "Description 1", measureX, measureY, quantity, material);
    //     quoteItem.calculate();
    //     assertEquals(new BigDecimal("0.3600"), quoteItem.getArea());
    //     assertEquals(new BigDecimal("306.0000"), quoteItem.getValue());
    //     assertEquals(new BigDecimal("0.7200"), quoteItem.getTotalArea());
    //     assertEquals(new BigDecimal("612.0000"), quoteItem.getTotalValue());
    // }
}
